# This workflow will build a .NET project
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-net

name: Build

on:
  workflow_dispatch:
  push:
    branches:
      - dev
    tags:
      - 'v*'
  pull_request:
    branches:
      - dev

permissions:
  contents: write

jobs:
  build:

    runs-on: windows-latest
    env:
      VersionPrefix: 2.1.3
      NUGET_CERT_REVOCATION_MODE: offline
    steps:
      - uses: actions/checkout@v7
      - name: Determine build version
        id: version
        shell: pwsh
        run: |
          if ("${{ github.ref_type }}" -eq "tag") {
            if ("${{ github.ref_name }}" -notmatch '^v(?<version>\d+\.\d+\.\d+)$') {
              throw "Release tags must use the format vMAJOR.MINOR.PATCH."
            }
            $buildVersion = $Matches.version
          }
          else {
            $buildVersion = "$env:VersionPrefix.${{ github.run_number }}"
          }

          $packageVersion = if ("${{ github.ref_type }}" -eq "tag") {
            $buildVersion
          }
          else {
            "$env:VersionPrefix-ci.${{ github.run_number }}"
          }

          "flow_version=$buildVersion" >> $env:GITHUB_OUTPUT
          "package_version=$packageVersion" >> $env:GITHUB_OUTPUT
          "flowVersion=$buildVersion" >> $env:GITHUB_ENV
          "flowPackageVersion=$packageVersion" >> $env:GITHUB_ENV
      - name: Update bundled plugin versions
        shell: pwsh
        run: .\Scripts\update_plugin_versions.ps1
      - name: Set Flow.Launcher.csproj version
        id: update
        uses: vers-one/dotnet-project-version-updater@v1.8
        with:
          file: |
            "**/SolutionAssemblyInfo.cs"
          version: ${{ steps.version.outputs.flow_version }}
      - name: Setup .NET
        uses: actions/setup-dotnet@v6
        with:
          dotnet-version: 9.0.x
#          cache: true
#          cache-dependency-path: |
#            Flow.Launcher/packages.lock.json
#            Flow.Launcher.Core/packages.lock.json            
#            Flow.Launcher.Infrastructure/packages.lock.json
#            Flow.Launcher.Plugin/packages.lock.json
      - name: Install vpk
        run: dotnet tool install -g vpk
      - name: Restore dependencies
        run: nuget restore
      - name: Build
        run: dotnet build --no-restore -c Release
      - name: Initialize Service
        run: |
          sc config WSearch start= auto # Starts Windows Search service- Needed for running ExplorerTest
          net start WSearch
      - name: Test
        run: dotnet test --no-build --verbosity normal -c Release
      - name: Perform post_build tasks
        shell: powershell
        run: .\Scripts\post_build.ps1
      - name: Upload Plugin Nupkg
        uses: actions/upload-artifact@v7
        with:
          name: Plugin nupkg
          path: |
            Output\Release\Flow.Launcher.Plugin.*.nupkg
          compression-level: 0
      - name: Upload Setup
        uses: actions/upload-artifact@v7
        with:
          name: Flow Installer
          path: |
            Output\Packages\Flow-Launcher-*.exe
          compression-level: 0
      - name: Upload Portable Version
        uses: actions/upload-artifact@v7
        with:
          name: Portable Version
          path: |
            Output\Packages\Flow-Launcher-Portable.zip
          compression-level: 0
      - name: Upload Full Nupkg
        uses: actions/upload-artifact@v7
        with:
          name: Full nupkg
          path: |
            Output\Packages\FlowLauncher-*-full.nupkg

          compression-level: 0
      - name: Upload Release Information
        uses: actions/upload-artifact@v7
        with:
          name: RELEASES
          path: |
            Output\Packages\RELEASES
          compression-level: 0
      - name: Publish GitHub Release
        if: github.ref_type == 'tag'
        uses: softprops/action-gh-release@v3
        with:
          name: Flow Launcher ${{ github.ref_name }}
          generate_release_notes: true
          prerelease: false
          fail_on_unmatched_files: true
          files: |
            Output\Packages\Flow-Launcher-*.exe
            Output\Packages\Flow-Launcher-Portable.zip
            Output\Packages\FlowLauncher-*-full.nupkg
            Output\Packages\RELEASES
